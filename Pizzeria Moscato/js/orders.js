let price = 0;
let total = 0;

const addCapricciosa = () => {
    let x = document.getElementById('total');
    price = 12.99;
    total += price;
    x.innerHTML = 'Total: <br>$' + total.toFixed(2);
}

const addMargharita = () => {
    let x = document.getElementById('total');
    price = 8.99;
    total += price;
    x.innerHTML = 'Total: <br>$' + total.toFixed(2);
}

const addHawaiian = () => {
    let x = document.getElementById('total');
    price = 6.99;
    total += price;
    x.innerHTML = 'Total: <br>$' + total.toFixed(2);
}

const addVegeterian = () => {
    let x = document.getElementById('total');
    price = 13.49;
    total += price;
    x.innerHTML = 'Total: <br>$' + total.toFixed(2);
}

const addMulberry = () => {
    let x = document.getElementById('total');
    price = 12.99;
    total += price;
    x.innerHTML = 'Total: <br>$' + total.toFixed(2);
}

const addRusticaChicken= () => {
    let x = document.getElementById('total');
    price = 15.00;
    total += price;
    x.innerHTML = 'Total: <br>$' + total.toFixed(2);
}

const addMeditarranean = () => {
    let x = document.getElementById('total');
    price = 16.50;
    total += price;
    x.innerHTML = 'Total: <br>$' + total.toFixed(2);
}

const addBBQChicken = () => {
    let x = document.getElementById('total');
    price = 16.00;
    total += price;
    x.innerHTML = 'Total: <br>$' + total.toFixed(2);
}